#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
	char string[100]; //Defining the char variable
	char rep[20][20];
    int i=0, j, rep;
    printf("Enter a text: "); //Enter the string
    gets(string); //reading the string
    puts(string); //printing the string
    for(i=0; i< 100; i++)
    {
    	while(string[i]!='\0')
    	{
    		//Separate words
    	}
    	//After separating the words - we need to compare them by using strcpm || strcmp 
		if(strcmp == true)
		{
			//dellete and add to the repeat array
		}
		else
		{
			//add to another array
		}
    	rep++;
    	//show the repeated words from repeated array
    	//show the repeating frequency
    }
    puts(l); //Print the final deleted string
}
